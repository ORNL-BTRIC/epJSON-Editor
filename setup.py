from setuptools import setup

from epjsoneditor import NAME, VERSION

setup(
    name=NAME,
    version=VERSION,
    description='EpJSON Editor',
    url='https://github.com/NREL/epJSON-Editor',
    license='',
)
